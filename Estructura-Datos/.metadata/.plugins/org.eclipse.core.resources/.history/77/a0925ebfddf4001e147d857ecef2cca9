package cursoED.actividades;

public class MCD {
	public static int mcd(int a, int b) {
		int menor = 0;
		int mcd = 0;
		if (a >= b ) {
			menor = b;
		}else {
			menor = a;
		}
		
		for(int i=0;i<=menor;i++) {
			if(a%i==0 && b%i==0) {
				mcd = i;
			}
		}
		return mcd;
	}
}
